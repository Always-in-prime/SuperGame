#pragma once
#include "Common.h"

// ============================================================
//  Ќамерени€ одного игрока за один тик физики.
//  «аполн€етс€ из Input (клавиатура), из AI (бот) или из теста.
//  World_Step не знает, откуда ввод.
// ============================================================

struct PlayerInput {
    double forward = 0.0;    // -1..+1  (W/S)
    double turn = 0.0;    // -1..+1  (A/D)
    bool   shoot = false;  // held
    bool   shootPressed = false; // edge (нажатие именно в этом тике)
};